---
phase: 05-validation-wrapper-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/verify-suite.sh
autonomous: true
requirements:
  - AUTO-01
user_setup: []
must_haves:
  truths:
    - Operators can run one repository-root wrapper command to execute baseline reliability checks.
    - Wrapper command exists at `./scripts/verify-suite.sh` and is tracked in git.
    - Wrapper performs core required checks for shell, tmux, vim, and docs/runtime parity in deterministic order.
  artifacts:
    - .gitignore
    - scripts/verify-suite.sh
  key_links:
    - .gitignore:scoped unignore rules -> scripts/verify-suite.sh tracked artifact
    - scripts/verify-suite.sh:required check catalog -> zsh/.zshrc + tmux/.tmux.conf + vim/.vimrc + README.md + AGENTS.md
---

<objective>
Implement the baseline Phase 5 wrapper command contract and required check catalog.

Purpose: establish a tracked, executable, repo-root verification wrapper for core reliability checks.
Output: `scripts/verify-suite.sh` plus ignore rules that ensure the wrapper is versioned.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-validation-wrapper-baseline/05-CONTEXT.md
@.planning/phases/05-validation-wrapper-baseline/05-RESEARCH.md
@.gitignore
@README.md
@AGENTS.md
@zsh/.zshrc
@tmux/.tmux.conf
@vim/.vimrc
@.planning/phases/01-installation-baseline/01-VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track wrapper artifact path and bootstrap command skeleton</name>
  <files>.gitignore,scripts/verify-suite.sh</files>
  <action>Add scoped `.gitignore` exceptions so `scripts/verify-suite.sh` is tracked while preserving the repo's broader ignore posture. Create executable `scripts/verify-suite.sh` with repo-root invocation guard, deterministic check order scaffolding, and required check runner helpers.</action>
  <verify>test -x scripts/verify-suite.sh && git check-ignore -v scripts/verify-suite.sh >/dev/null 2>&1 && exit 1 || true</verify>
  <done>`scripts/verify-suite.sh` exists, is executable, and appears as a trackable file in git status.</done>
</task>

<task type="auto">
  <name>Task 2: Implement required core check catalog for wrapper baseline</name>
  <files>scripts/verify-suite.sh</files>
  <action>Implement required check execution for shell syntax, tmux config load, vim startup (normal and temp-home), and docs/runtime parity pattern checks. Keep command execution non-interactive and deterministic. Ensure wrapper runs from repo root and does not execute `install.sh` by default.</action>
  <verify>./scripts/verify-suite.sh >/tmp/verify-suite.out 2>&1; status=$?; rg -n "PASS|FAIL|SKIP|SUMMARY" /tmp/verify-suite.out && test "$status" -eq 0</verify>
  <done>Wrapper executes required checks in fixed order and exits successfully when required checks pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -x scripts/verify-suite.sh` passes
- [ ] `git check-ignore scripts/verify-suite.sh` returns non-zero (not ignored)
- [ ] `./scripts/verify-suite.sh` from repo root executes and prints per-check statuses
- [ ] `rg -n "zsh/.zshrc|tmux/.tmux.conf|vim/.vimrc|README.md|AGENTS.md" scripts/verify-suite.sh` confirms required coverage scope
</verification>

<success_criteria>

- AUTO-01 is satisfied with a stable repo-root wrapper command contract
- Wrapper artifact is tracked in repository history
- Required baseline checks are executable and deterministic

</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-wrapper-baseline/05-01-SUMMARY.md`
</output>
