---
phase: 04-documentation-and-release-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - CHANGELOG.md
  - VERSION
  - README.md
autonomous: true
requirements:
  - DOCS-02
user_setup: []
must_haves:
  truths:
    - `CHANGELOG.md` includes a structured `2.1.1` release entry describing delivered reliability/documentation alignment.
    - Historical changelog entries are preserved, with additive legacy context notes instead of rewrites.
    - `VERSION` and user-facing version references are consistent at `2.1.1`.
  artifacts:
    - CHANGELOG.md
    - VERSION
    - README.md
  key_links:
    - CHANGELOG.md:2.1.1 release entry -> VERSION value and Phase 4 delivered artifacts
    - CHANGELOG.md:legacy context note -> preservation of 2.0.0 historical sections
    - README.md:version badge -> VERSION file consistency
---

<objective>
Finalize release metadata for Phase 4 by aligning `CHANGELOG.md`, `VERSION`, and version references.

Purpose: Deliver reliable release hygiene with historical integrity and explicit traceability.
Output: Patch release metadata set to `2.1.1` with structured changelog coverage.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-documentation-and-release-hygiene/04-CONTEXT.md
@.planning/phases/04-documentation-and-release-hygiene/04-RESEARCH.md
@CHANGELOG.md
@VERSION
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured 2.1.1 changelog entry with legacy context policy</name>
  <files>CHANGELOG.md</files>
  <action>Add a new `2.1.1` changelog entry with structured categories and file/contract-level specificity for the shipped reliability + documentation alignment work. Preserve historical entries and add an explicit legacy-context note for older broad tooling references without rewriting their content.</action>
  <verify>rg -n "^## \\[2\\.1\\.1\\]|legacy|historical|README|AGENTS|VERSION|reliability" CHANGELOG.md</verify>
  <done>Changelog contains a clear `2.1.1` entry and additive legacy context note while preserving prior release records intact.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version and align user-facing version references</name>
  <files>VERSION, README.md</files>
  <action>Set `VERSION` to `2.1.1` and align user-facing version references (including README version badge text) so release metadata stays consistent after Phase 4 docs reconciliation.</action>
  <verify>test \"$(cat VERSION)\" = \"2.1.1\" && rg -n \"2\\.1\\.1\" VERSION README.md CHANGELOG.md</verify>
  <done>VERSION, changelog release header, and README version display are consistent at `2.1.1`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test "$(cat VERSION)" = "2.1.1"` passes
- [ ] `rg -n "^## \\[2\\.1\\.1\\]" CHANGELOG.md` finds exactly one latest release header
- [ ] `rg -n "2\\.1\\.1" README.md VERSION CHANGELOG.md` confirms cross-file consistency
- [ ] `rg -n "^## \\[2\\.0\\.0\\]|^## \\[1\\.0\\.0\\]" CHANGELOG.md` confirms historical entries remain present
</verification>

<success_criteria>

- DOCS-02 is satisfied with consistent release metadata
- Historical changelog integrity is preserved
- Release target reflects locked context decision (`2.1.1`)

</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-and-release-hygiene/04-02-SUMMARY.md`
</output>
