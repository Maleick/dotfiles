---
phase: 04-documentation-and-release-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []
must_haves:
  truths:
    - `README.md` and `AGENTS.md` reflect shipped command/keybinding/runtime behavior without stale claims.
    - README includes a dedicated verification checklist section covering install, shell, tmux, vim, and docs/release integrity checks.
    - Documentation behavior claims are anchored to current source files (`zsh/.zshrc`, `tmux/.tmux.conf`, `vim/.vimrc`, `install.sh`).
  artifacts:
    - README.md
    - AGENTS.md
  key_links:
    - README.md:verification sections -> zsh/.zshrc + tmux/.tmux.conf + vim/.vimrc + install.sh
    - AGENTS.md:tmux architecture notes -> tmux/.tmux.conf fail-soft/logging/keybinding contracts
    - AGENTS.md:vim architecture notes -> vim/.vimrc startup guard and fallback behavior
---

<objective>
Reconcile `README.md` and `AGENTS.md` to current runtime behavior and documentation standards from Phase 4 context.

Purpose: Ensure operator-facing docs are accurate, executable, and contract-driven.
Output: Updated docs with runtime-aligned guidance and a dedicated verification checklist model.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-documentation-and-release-hygiene/04-CONTEXT.md
@.planning/phases/04-documentation-and-release-hygiene/04-RESEARCH.md
@README.md
@AGENTS.md
@zsh/.zshrc
@tmux/.tmux.conf
@vim/.vimrc
@install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile README to runtime behavior and dedicated verification model</name>
  <files>README.md</files>
  <action>Update README to comprehensively match shipped command/keybinding/runtime behavior and add or refine a dedicated verification checklist section covering install, shell, tmux, vim, and docs/release integrity checks. Preserve operator-facing command examples and avoid adding new runtime capabilities.</action>
  <verify>rg -n "Baseline Verification|Shell Reliability Verification|Tmux|Vim|docs/release|Prefix \\+ P|Prefix \\+ U / K|base64decode|quickscan" README.md</verify>
  <done>README behavior claims are source-grounded and verification guidance is explicit, copy/paste-ready, and phase-complete.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile AGENTS architecture notes to shipped contracts</name>
  <files>AGENTS.md</files>
  <action>Update AGENTS guidance to align with current shell/tmux/vim implementation details, including tmux fail-soft recording guards, logging path behavior, and Vim startup/plugin fallback guards. Keep contract language explicit for future agents.</action>
  <verify>rg -n "fail-soft|Prefix \\+ P|~/Logs|Prefix \\+ U|Prefix \\+ K|vim-plug|fallback|coc|WARP_TERMINAL|aliasr" AGENTS.md</verify>
  <done>AGENTS reflects current implementation and preserves strict future-agent constraints without stale architecture claims.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Prefix \\+ P|Prefix \\+ S|Prefix \\+ U|Prefix \\+ K|~/Logs|asciinema|fzf" README.md AGENTS.md` confirms tmux contract coverage
- [ ] `rg -n "vim-plug|fallback|coc|catppuccin|dracula|molokai" README.md AGENTS.md` confirms Vim startup/fallback contract coverage
- [ ] `rg -n "quickscan|webserver|http-server|https-server|base64decode|myip|netinfo" README.md AGENTS.md` confirms shell helper behavior coverage
</verification>

<success_criteria>

- DOCS-01 is satisfied with no stale behavior documentation
- Verification checklist model is explicit and operator-usable
- Docs remain scoped to shipped behavior (no feature expansion)

</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-and-release-hygiene/04-01-SUMMARY.md`
</output>
